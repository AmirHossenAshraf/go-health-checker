# go-health-checker
A fast, concurrent API health checker CLI tool written in Go. Monitor multiple endpoints simultaneously with configurable intervals, timeout handling, and beautiful terminal output.
